<!DOCTYPE html>
<html>
	<head>
		<title><%= title %></title>
		<link rel='stylesheet' href='/stylesheets/style.css' />
	</head>
	<body>
		<h1><%= title %></h1>
		<% if (errors.length > 0) { %> 
		<div id="errors">
			<h2><%= errors.length %> Stations not responding:</h2>
			<ul>
				<% for(var i = 0; i < errors.length; i++) { %>
					<li>
						<%= errors[i] %>
					</li>
				<% } %>
			</ul>
		</div>
		<% } %>
		<h2>Number of stations: <%= stations.length %></h2>
		<hr />
		<% for(var i = 0; i < stations.length; i++) { %>    
			<div>
				<h2><%= stations[i].calls %></h2>
				<img src="<%= stations[i].image %>" />
				<ul>
						<li>Station ID: <%= stations[i].stationId %></li>
						<li>Timestamp: <%= stations[i].timestamp %></li>
						<li>Artist: <%= stations[i].artist %></li>
						<li>Title: <%= stations[i].title %></li>
						<li>Text: <%= stations[i].text %></li>
				</ul>
				<hr />
			</div>
		<% } %>
		
	</body>
</html>
